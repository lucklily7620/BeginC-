#include <bits/stdc++.h>
//it use all pac. when only cout and endl, just include <iostream> ok
using namespace std;
//def namespace = std, lock

int main() {
  cout << "Hello, world!" << endl;
  cout << "cout, mean C OUT" << endl;
  cout << endl;
  cout << "it can " << "sepa " << "or " << "conec ";
  cout << endl << endl;

  cout << "if A > 0" << " ... A*B > 0 " << endl;
  cout << "if A < 0" << " ... A*B < 0 " << endl;
}
